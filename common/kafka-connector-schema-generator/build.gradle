/*
 * This file was generated by the Gradle 'init' task.
 *
 * This project uses @Incubating APIs which are subject to change.
 */

dependencies {
    api("${lib.kafka_protobuf_provider}") {
        exclude(group: 'org.apache.kafka', module: 'kafka-clients')
        exclude(group: 'io.confluent', module: 'common-utils')
        exclude(group: 'io.confluent', module: 'kafka-schema-registry-client')
        exclude(group: 'com.google.protobuf', module: 'protobuf-java')
        exclude(group: 'com.google.protobuf', module: 'protobuf-java-util')
        exclude(group: 'com.squareup.okio', module: 'okio-jvm')
        exclude(group: 'com.squareup.wire', module: 'wire-runtime-jvm')
        exclude(group: 'org.slf4j', module: '*')
        exclude(group: 'com.google.guava', module: 'guava')
        exclude(group: 'com.fasterxml.jackson.dataformat', module: 'jackson-dataformat-csv')
    }
    api "${lib.ali_transmittable_thread_local}"
    api "${lib.caffeine_cache}"
    api "${lib.hutool_core}"
    api("${lib.json_schema_inferrer}")
    api("${lib.json_schema_everit}") {
        exclude(group: 'commons-collections', module: 'commons-collections')
    }
    api("${lib.kafka_clients}") {
        exclude(group: 'org.slf4j', module: '*')
    }
    api("${lib.kafka_schema_registry_client}") {
        exclude(group: 'org.apache.kafka', module: 'kafka-clients')
        exclude(group: 'org.apache.avro', module: 'avro')
        exclude(group: 'com.google.guava', module: 'guava')
        exclude(group: 'io.confluent', module: 'common-utils')
        exclude(group: 'org.apache.commons', module: 'commons-lang3')
        exclude(group: 'org.yaml', module: 'snakeyaml')
    }
    api("${lib.kafka_schema_serializer}") {
        exclude(group: 'org.apache.kafka', module: 'kafka-clients')
        exclude(group: 'io.confluent', module: 'kafka-schema-registry-client')
        exclude(group: 'io.confluent', module: 'common-utils')
        exclude(group: 'org.slf4j', module: '*')
    }
    api("${lib.apache_http_components_core}")
    api("${lib.apache_avro}") {
        exclude(group: 'org.slf4j', module: '*')
    }
    compileOnly("${lib.apache_http_components_core}")
    compileOnly("${lib.jackson_databind}")
    compileOnly("${lib.kafka_json_schema_provider}")
    compileOnly "${lib.junit_jupiter}"
}

description = 'kafka-connector-schema-generator'
